import { HexColor } from "../../../types/HexColor";

/**
 * Using this still requires you to set `layerBgSolid` on your own
 */
export const defaultLayers = {
  layerBg: "#ffffff11" as HexColor,
  layerBgHighlight: "#ffffff22" as HexColor,
};
